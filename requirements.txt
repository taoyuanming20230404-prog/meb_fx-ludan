# 分销录单脚本 fx_ludan.py / 离线匹配测试 test_project_match.py 依赖
# 安装：pip install -r requirements.txt

# 核心依赖（必装）
pandas>=2.0.0
openpyxl>=3.1.0
selenium>=4.15.0
rapidfuzz>=3.0.0

# 可选依赖（不装也能运行，仅影响部分功能）
# pyautogui>=0.9.54
# pywin32>=306
